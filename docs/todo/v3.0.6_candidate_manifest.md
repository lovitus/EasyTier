# v3.0.6 Candidate Manifest

## Candidate lifecycle

- Branch: `codex/v3.0.6-release`
- Parent candidate: `1d999fd7540558abe2c4b4162e4425ff68100c91`
- Parent formal workflows:
  - Core `30373790096`: `FAIL` on 32-bit MIPS because the interface snapshot
    cache used `std::sync::atomic::AtomicU64`;
  - GUI `30373794219`: `PASS`;
  - Mobile `30373797705`: `PASS`;
  - OHOS `30373801947`: `PASS`;
  - Test `30373805870`: `FAIL` on six deterministic Clippy diagnostics.
- Parent disposition: `REJECTED`.

The final commit SHA and post-build workflow evidence are recorded only after this
immutable source snapshot is committed and built.

## Intended build snapshot

Build-affecting changes:

- `easytier/src/common/network.rs`
  - use the existing cross-platform `atomic_shim::AtomicU64` for snapshot
    generations so 32-bit MIPS retains the same `u64` generation semantics;
  - apply Clippy-equivalent test construction changes.
- `easytier/src/common/global_ctx.rs`
  - apply the Clippy-equivalent conditional form without changing invalidation
    events.
- `easytier/src/common/underlay_guard.rs`
- `easytier/src/connector/mod.rs`
  - replace one-element cloned slices with borrowed one-element slices in tests.
- `easytier-policy/src/windows.rs`
  - stop using public destinations such as `1.1.1.1` as route-readiness probes;
  - enumerate the local Windows route table and require the Leaf Wintun to own
    both IPv4 `/1` capture routes and, when enabled, both IPv6 `/1` routes;
  - permit legitimate more-specific DNS, proxy, mesh, and LAN bypass routes;
  - fail closed when a required route is missing or the same capture prefix is
    also owned by another interface;
  - add pure ownership tests for success, missing routes, competing routes, and
    more-specific bypass routes.

Non-build changes:

- this candidate manifest.

Explicitly unchanged:

- Leaf and Mihomo packet, DNS, proxy, rule, and lifecycle semantics;
- long-running Mihomo and GOST guardian ownership;
- dependencies, `Cargo.lock`, generated files, and workflow pins.

## Mandatory pre-push gates

Remote `.160` debug builder:

- `cargo test --locked --no-run --package easytier --lib`: `PASS`
- compiled test binary: `easytier-30536129777d6544`
- focused interface snapshot/underlay/connector tests: `11 PASS`, `0 FAIL`
- affected-package full-feature Clippy:
  `cargo clippy --locked --all-targets --features full --package easytier
  --package easytier-policy -- -D warnings`: `PASS`
- exact workspace Clippy command reached the unchanged GUI build script and
  stopped because the debug builder does not contain the workflow-prepared Leaf
  sidecar. The formal Test workflow remains the authoritative workspace-level
  Clippy gate.

Windows native gate:

- `easytier-policy` native compile on Rust 1.94: `PASS`
- route-table ownership unit tests: `2 PASS`, `0 FAIL`
- the complete Core command cannot be used as a native artifact gate on this
  host: `full` requires a Perl-based vendored OpenSSL build that is not
  installed, while the reduced command reaches an existing `rust_i18n` macro
  failure under Rust 1.94. The project and formal Windows workflow use Rust
  1.95.
- compile the EasyTier caller with Rust 1.95 in the formal GUI/Core workflow:
  `PENDING`
- start policy routing on the machine that reproduced Windows error `1232`,
  then confirm policy TUN, DIRECT/proxy traffic, stop, and cleanup from the
  exact formal Windows artifact: `PENDING` and required before Release.

## Required candidate and formal workflows

After all pre-push gates pass:

1. Push one immutable `[skip ci]` candidate.
2. Run only `EasyTier GUI macOS ARM64 Test`.
3. Confirm the exact macOS artifact still starts, routes traffic, and cleans up.
4. Run Core, GUI, Mobile, OHOS, and Test against the same SHA.
5. Inspect formal Core and GUI artifacts before `EasyTier Release`.

## Planned validation evidence

- Core: all 32-bit and 64-bit target jobs compile, including MIPS.
- Test: Clippy and the complete test workflow pass.
- Windows: fixed-destination reachability is no longer a startup dependency;
  exact capture prefixes remain fail-closed and policy traffic works.
- macOS: validator reaping remains correct; the accepted upstream Mihomo
  `dscacheutil` zombie limitation is documented after release and is not treated
  as an EasyTier lifecycle regression.

## Build wait tasks

- inspect the complete diff, `Cargo.lock`, platform `cfg`, workflow pins, and
  generated files;
- prepare the Windows route-table and traffic checks;
- do not mutate the immutable candidate while a workflow is running;
- do not trigger a second build for post-build evidence or documentation.
