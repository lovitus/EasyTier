# v3.0.6 Candidate Manifest

## Candidate lifecycle

- Branch: `codex/v3.0.6-release`
- Parent candidate: `d7be2a12e4a134da5833e2de6fc3ae3b90931bc0`
- Parent macOS ARM64 workflow: `30351180907`
- Parent disposition: `REJECTED`
- Rejection evidence: the exact installed artifact left a Core-owned short-lived
  validator/guardian descendant in a persistent zombie state. The process remained
  defunct for more than eight minutes. Long-running Mihomo and GOST supervision,
  policy traffic, underlay recovery, and interface-cache CPU evidence remained
  functional.

The final commit SHA and post-build workflow evidence are recorded only after this
immutable source snapshot is committed and built.

## Intended build snapshot

Build-affecting changes:

- `easytier/src/mihomo.rs`
  - run short-lived `mihomo -t` validation without the long-running sidecar
    guardian;
  - retain bounded validation timeout;
  - explicitly kill and wait for a timed-out validator;
  - preserve bounded stdout/stderr diagnostics and existing validation errors;
  - add normal-exit and timeout reaping contract tests.

Non-build changes:

- this candidate manifest.

Explicitly unchanged:

- long-running Mihomo and GOST `ManagedChild` guardian ownership;
- Mihomo source/runtime configuration generation;
- TUN, routing, DNS, controller, dashboard, proxy, and mesh behavior;
- dependencies, `Cargo.lock`, generated files, and workflow pins.

## Mandatory remote preflight

Remote debug builder:

- `cargo test --locked --no-run --package easytier --lib`: `PASS`
- compiled test binary: `easytier-30536129777d6544`
- `mihomo_validator_lifecycle_tests`: `2 PASS`, `0 FAIL`
- `editor_validation_rejects_yaml_and_validator_failures_without_starting`:
  `1 PASS`, `0 FAIL`
- compiler warnings introduced by this batch: none

## Required candidate workflow

Only the macOS ARM64 candidate workflow is permitted for this pre-release fix.
Formal Core, GUI, Mobile, OHOS, Test, tags, and Release remain blocked until the
exact candidate artifact passes maintainer-host validation.

## Planned exact-artifact evidence

- verify artifact SHA, target, architecture, signatures, Mihomo/GOST payloads,
  and internal version;
- validate Mihomo YAML, start, DIRECT/proxy traffic, dashboard, and source-file
  immutability;
- repeat Mihomo and GOST crash/restart cycles;
- verify no Core-owned validator, sidecar, or guardian zombies remain;
- verify stop/start cleanup and Core FD/thread return toward baseline;
- confirm the interface-cache CPU/syscall improvement remains present.

## Build wait tasks

- prepare bounded lifecycle commands that tolerate transient proxy recovery;
- distinguish Core-owned children from Mihomo-owned descendants;
- preserve the rejected parent artifact and its diagnostic evidence;
- do not modify the immutable candidate while its workflow is running.
