# v3.0.7 Candidate Manifest

## Scope

Branch: `codex/v3.0.7-release`.

Base release: `v3.0.6`,
`a8a11c076c75a109c1fd882a32b206a60b0bf60f`.

Superseded candidate:

- `94f399aa41d6882c11601def5e11d165351f7283`: `REJECTED`;
- Mobile run `30422616280` proved that Android compiled the desktop-only
  Zashboard window API;
- Core, GUI, and Test were cancelled after diagnosis; the successful OHOS run
  does not transfer to the replacement candidate.
- `0fe13ead5bd92ef279fd1761e2cfe11b045e0752`: `REJECTED`;
- Mobile `30424192041`, OHOS `30424194008`, and every Test execution shard
  passed, but Test `30424195909` failed the `-D warnings` Clippy gate on a
  collapsible match; Core and GUI were cancelled after diagnosis.

Immutable candidate:

- commit: `50cf3de5a6179a05fd574f802814c154212b4b56`;
- tree: `450ee10d4b5ee5169e9bfc7d8c558644556f1ae7`;
- branch: `codex/v3.0.7-release`.

Build-affecting changes:

- replace the GUI's system-browser Zashboard launch with one managed Tauri
  webview window;
- reuse and focus that window on repeated requests;
- preserve close-to-background behavior only for the main window;
- close an unfocused Zashboard window after a bounded 15-minute, event-driven
  timeout;
- retain the same command surface on Android/iOS with an explicit unsupported
  result, without compiling desktop window APIs into mobile targets;
- align stale frontend tests with the already-released explicit backend and
  CodeMirror editor contracts without changing production behavior;
- update workspace, GUI, Web, Tauri, lockfile, and release metadata to 3.0.7.

Explicitly unchanged:

- Core packet, mesh, routing, DNS, policy, Mihomo, GOST, and Leaf semantics;
- Mihomo controller URL and runtime-secret generation;
- Android and OHOS behavior;
- dependency and sidecar pins.

## Code-review contracts

- Only the `main` window intercepts close requests and hides in the background.
- `mihomo-zashboard` and future auxiliary windows close normally.
- Focus restores cancel older idle-close generations.
- Only the current unfocused generation may close the dashboard.
- No polling loop is introduced.
- The dashboard label is absent from Tauri capability windows, so the remote
  dashboard cannot invoke main-window plugin permissions.

## Mandatory pre-push gates

Remote `.160`:

- locked Rust GUI no-run build: `PASS`; initial full build completed in
  3m55s and the mobile-cfg replacement candidate completed incrementally in
  6.22s;
- exact test binary: `app_lib-a8dedba0386d0733`;
- focused dashboard-window policy tests: `4 PASS`, `0 FAIL`;
- GUI `cargo clippy --locked --all-targets -- -D warnings`: `PASS`,
  completed in 3m01s;
- frontend-lib config/UI tests: `120 PASS`, `0 FAIL`;
- frontend-lib, Web frontend, VPN service plugin, GUI typecheck, and GUI
  production builds: `PASS`;
- lockfile, version, platform `cfg`, capability, and complete-diff audit:
  `PASS`.

Formal workflows for the one immutable candidate:

- Core: `30426581269`, `PASS`;
- GUI: `30426583307`, `PASS`;
- Mobile: `30426585308`, `PASS`;
- OHOS: `30426587089`, `PASS`;
- Test: `30426588901`, `PASS`.

## Exact-artifact validation

- Windows: open Zashboard repeatedly without the former DLL/1223 error; confirm
  singleton reuse, focus, manual close, and unchanged main-window behavior.
- macOS: open, reuse, close, and reopen Zashboard; confirm the main GUI remains
  available and Mihomo stays running.
- Package audit: version 3.0.7, target architecture, Mihomo/GOST payloads, and
  existing release provenance checks.

Any build-affecting change after candidate commit invalidates these gates and
requires a new candidate. Post-build evidence updates do not.

## Release evidence

- macOS ARM64 exact GUI artifact: `PASS`; version 3.0.7, ARM64 Mach-O,
  strict code-signature validation, packaged Mihomo/GOST payloads, restored
  normal-user configuration, Mihomo/GOST/controller startup, repeated
  Zashboard reuse, manual close, and unchanged main GUI/network lifecycle;
- Windows exact runtime interaction: `WAIVED_BY_MAINTAINER`; the exact
  Windows GUI workflow, packaging, architecture, and payload gates passed,
  while the external-browser path that caused DLL/1223 errors is no longer
  used;
- release workflow: `30431643939`, `PASS`;
- tag and latest Release: `v3.0.7`, both target
  `50cf3de5a6179a05fd574f802814c154212b4b56`;
- published assets: `46`, all uploaded;
- published macOS ARM64 DMG SHA-256:
  `34cabdb32fb877f088c9dcfd8d1e4802bdb721699ab74c450a93f8b87936905f`,
  identical to the exact GUI artifact validated on macOS.
