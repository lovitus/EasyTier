[package]
name = "mesh-recv-budget"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
async-ringbuf = "=0.3.1"
futures = "=0.3.30"
tokio = { version = "=1.52.1", features = ["rt", "rt-multi-thread", "sync", "time"] }
