# v3.0.16-3 Candidate Manifest

- Status: PREBUILD_GATE_PASSED; IMMUTABLE_COMMIT_PENDING
- Candidate SHA: pending immutable commit
- Scope: harden auxiliary bootstrap-peer persistence and make normal Core/GUI/Windows-service
  shutdown explicitly persist the final live snapshot.
- Leaf SHA: `682d1dc43585a703c993e8875fe4e937b1038733`
- HEV SHA: `97e74f1068bd924e740032382cdc94ca83741ae6`
- Android candidate: required
- Preflight: `scripts/release-operator.sh prepare`
- Validation: `scripts/release-operator.sh validate`

## Frozen implementation scope

- Maintain exactly one primary bootstrap URL file and one fixed backup per stable network identity.
- Never replace a useful primary with an empty, identical, or strict-subset snapshot.
- Prefer the primary at startup and use the backup only when the primary has no valid URL.
- Reject `ring://`, malformed URLs, and tunnel schemes unsupported by the current build.
- Append recovered URLs only to the in-memory initial connector list; never modify user
  configuration.
- Preserve the v3.0.16-2 manual-connector retry and recovery semantics. The attempted
  manager-wide cooldown is excluded because formal tests proved that it delayed eventless recovery
  and destabilized existing P2P-only coverage.
- Persist through the existing manager shutdown path on normal Core signal, GUI Exit, Android
  stop, and Windows Service Stop.
- Keep the bootstrap state directory independent from Policy/Geo/Mihomo resource directories when
  Core runs without `--config-dir`.

## Pre-build evidence

- Previous preflight evidence was invalidated by removing the failed cooldown candidate.
- On 2026-08-11, the first replacement-tree `scripts/release-operator.sh prepare` invocation
  completed the locked no-run build but stopped because `leaf-remote-preflight.sh` still named two
  tests from before the runtime-liveness/persistence split. No product test failed. The script-only
  correction replaced those stale filters and added the existing backup and `ring://` boundary
  tests; no assertion, timeout, or product source changed.
- The same complete replacement tree then passed `scripts/release-operator.sh prepare`: locked
  Rust no-run, the full maintained Leaf/HEV focused suite, quinn-udp 8/8, frontend-lib Vitest
  122/122, frontend-lib/frontend production builds, VPN plugin build, and GUI production build.
- `connector/manual.rs`, `connector/udp_hole_punch/mod.rs`, and `tests/three_node.rs` are byte-for-
  byte identical to released `v3.0.16-2`; the original P2P-only and eventless recovery assertions
  are therefore preserved.
- Linux-hosted MSVC cross-check: environment BLOCKED before EasyTier compilation because `.160`
  has no MSVC C toolchain. Windows runtime validation is outside this release's requested manual
  validation scope; the formal Core and GUI workflows remain mandatory compile/package gates.

## 64-bit atomic portability classification

- The exact lockfile pins `atomic-shim 0.2.0`. MIPS/MIPSel-reachable production
  `AtomicU64` uses in `p2p_endpoint_retry`, `proxy_failover`, `token_bucket`, `network`,
  `secure_datagram`, `peer_task`, `peer_ospf_route`, and `stealth` use that shim and only
  its supported fallback operations.
- Standard-library `AtomicU64` occurrences in `peer_conn.rs` and `tunnel/common.rs` are
  test-only.
- The standard-library `AtomicU64` in `virtual_nic.rs` is gated by
  `leaf-policy-proxy` on Unix and is excluded from both formal MIPS targets.
- The only `fetch_max` occurrence is `AtomicVersion(AtomicU32)` in
  `peer_ospf_route.rs`; there is no `atomic_shim::AtomicU64::fetch_max` call.
- Formal Core remains the non-waivable target proof: both exact-SHA MIPS and MIPSel jobs
  and their artifact architecture/endianness audits must pass before Release.

## Candidate workflows and exact-artifact evidence

- Run Linux Profiling Beta and Android Policy Candidate together for the immutable SHA.
- Linux: verify outer/inner checksums, `BUILD_INFO`, version, bootstrap through a cached peer after
  the configured initial peer is unavailable, disconnected Stop preservation, quick stop/start,
  lifecycle cleanup, and ordinary mesh traffic.
- Android: install the exact candidate, verify normal join and Stop persistence, restart through a
  cached peer without changing the saved configuration, quick stop/start preservation,
  process/VPN cleanup, and ordinary mesh traffic.
- While candidates build, prepare Linux hosts and the Android device; do not mutate the candidate.
- After both platform checks pass, run Core, GUI, Mobile, OHOS, and Test together, audit their
  exact-SHA artifacts, then publish once.

Post-build run IDs, hashes, measurements, and release evidence belong in the validation matrix and
must not redefine the candidate SHA.
