# v3.0.2 Candidate Manifest

- Release base: `v3.0.1` / `1b1ffcdaff1b004bc60ea03a87d0660b22a660f1`.
- Implementation candidate: `9e25583e5934195a1cf3dd98a5cd7d9caf7d57a5`.
- Final release candidate SHA: the immutable commit containing the `3.0.2`
  version metadata and release notes.
- Leaf SHA: `013a1497dd29355a00cd776628ff2de72e02e861`.
- HEV SHA: `97e74f1068bd924e740032382cdc94ca83741ae6`.
- Version: `3.0.2`.

## Included behavior

- Adds an isolated, explicit-only `quic-brutal://` overlay using local
  sender-side `tx_bps` configuration.
- Uses ordinary BBR when `tx_bps` is omitted and leaves all existing `quic://`
  configuration and behavior unchanged.
- Reuses existing listener, connector, endpoint, transport selection and failure
  handling without introducing overlay-specific fallback logic.
- Adds GUI listener configuration on preconfigured port `11013`.
- Upgrades `quinn-udp` to `0.5.15` and correctly handles a short final Stealth
  QUIC GSO segment.

## Dispatch lock

- Run the mandatory remote `--locked` no-run build for the complete `3.0.2`
  snapshot and the focused QUIC tests on `192.168.2.160`.
- Inspect version declarations, `Cargo.lock`, platform `cfg`, workflow pins,
  generated files and the complete candidate diff.
- Freeze and push one exact `releases/3.0.2` SHA.
- Run Core, GUI, Mobile, OHOS and Test against that same SHA, then run
  `EasyTier Release` with `v3.0.2`.
- Do not repeat completed performance or real-device validation for the version
  metadata-only release commit. Add the detailed CLI guide after publication as
  a documentation-only `[skip ci]` commit.

## Pre-build evidence

- The complete `3.0.2` snapshot was synchronized to the dedicated remote builder.
- `cargo test --locked --no-run --package easytier --lib
  --no-default-features --features quic,stealth-aead` passed using all available
  builder cores and the project Rust toolchain.
- The resulting exact test binary passed `tunnel::quic::tests::` 23/23 serially.
- Focused URL input tests passed 3/3. `frontend-lib`, Web frontend,
  `tauri-plugin-vpnservice` declarations and GUI production builds all passed in
  dependency order with Node 22.
- No additional network, performance or physical-device validation is required
  for this version-metadata-only release commit; the implementation evidence is
  tied to exact SHA `9e25583e5934195a1cf3dd98a5cd7d9caf7d57a5`.
