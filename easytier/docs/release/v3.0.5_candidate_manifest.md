# v3.0.5 Candidate Manifest

- Release base: `v3.0.4` / `5823fb5ea3804c098b9b19c3e339c12fdc29b12d`.
- Final release candidate SHA: assigned by the candidate commit after this
  complete versioned snapshot passed mandatory remote preflight.
- Leaf SHA: `013a1497dd29355a00cd776628ff2de72e02e861`.
- HEV SHA: `97e74f1068bd924e740032382cdc94ca83741ae6`.
- Version: `3.0.5`.

## Intended build snapshot

- On Android VPN ownership revocation, disable the exact owning non-`no_tun`
  instance through the existing network-state lifecycle so core, policy and
  persisted enabled state stop together.
- Reconcile the same cleanup from the native persisted revoke marker when the
  WebView was suspended during the callback.
- Treat an absent mesh-node route as actor availability rather than policy
  invalidity. Start the local Leaf bridge with an unavailable target so other
  actors remain usable; retain the existing route-generation refresh behavior.
- Add focused tests for partial mesh endpoint resolution and an initially
  unavailable remote slot. No GUI layout, rule ordering, normal mesh dialing,
  `no_tun` lifecycle, Leaf/HEV dependency pin or transport behavior changes.
- Include `3.0.5` version metadata, release notes and release-audit pins.

The unrelated local `qt_lite_service_console.md` and
`upstream_7_commits_selective_integration_plan.md` remain outside this candidate.

## Mandatory pre-build evidence

- The unversioned implementation snapshot passed
  `scripts/leaf-remote-preflight.sh` on the dedicated `.160` builder: locked
  EasyTier, policy and netstack library no-run targets plus the complete serial
  focused suite succeeded.
- Both new focused Rust tests passed on the exact remote test binary.
- Frontend Vitest, frontend-lib, Web frontend, `tauri-plugin-vpnservice` and
  `easytier-gui` production builds passed in dependency order on `.160`.
- The final versioned snapshot must repeat the same locked Rust gate and frontend
  dependency-order gate before it is committed or pushed.

## Final versioned preflight

- The complete `3.0.5` snapshot passed the `.160` locked no-run build. The exact
  EasyTier library test binary was `easytier-f755b76f4a975b4f`; the policy and
  netstack binaries were `easytier_policy-5481477f53b7929e` and
  `netstack_smoltcp-7d3834b0fcf73e6b`.
- The complete configured serial focused suite passed, including both new mesh
  actor regression tests. Runtime route output reported version `3.0.5`.
- Frontend Vitest, frontend-lib, Web frontend, `tauri-plugin-vpnservice` and
  `easytier-gui` production builds passed in dependency order. The remote Linux
  optional dependency was restored from the unchanged frozen lockfile after
  snapshot synchronization; no lockfile was regenerated.

## Candidate and validation plan

- Publish the immutable candidate only to `codex/profiling-beta`; rely on its
  automatic Linux profiling and Android policy candidate workflows without
  duplicate dispatches.
- Verify checksums, `BUILD_INFO`, exact SHA, build ID, symbols and target before
  deploying artifacts.
- Validate Linux compatibility, lifecycle, policy startup with a missing mesh
  actor, selected-actor failure and cleanup on the exact artifact.
- Android physical validation must confirm another VPN takeover stops the owning
  EasyTier instance and policy status, preserves `no_tun`, and does not reclaim
  VPN ownership automatically. The Android device is currently unavailable, so
  this gate remains blocked unless the maintainer explicitly makes it available
  or explicitly waives the gate for this exact candidate.
- Only after exact-artifact validation and maintainer confirmation, freeze
  `releases/3.0.5`, run Core, GUI, Mobile, OHOS and Test for one exact SHA, then
  run EasyTier Release for `v3.0.5`.
