# v3.0.5 Validation Matrix

| Gate | State | Exact evidence |
| --- | --- | --- |
| Previous candidate `046ce631` | REJECTED | Profiling payload was policy-capable, but formal Core/GUI payloads omitted required features or sidecars. Its successful workflow and Linux results are retained only as diagnostic evidence. |
| Previous replacement `316bfb69` | SUPERSEDED | Its Linux, Android and macOS exact-artifact checks passed and remain valid historical evidence, but they do not validate the later desktop Leaf/rust-tun changes. |
| Frozen replacement `f45fad64` | REJECTED | Exact Linux and Android candidate checks passed, but formal GUI failed on the Windows policy path because `Duration` was unavailable under Windows `cfg`. Test/Clippy independently failed because the static Tauri check did not prepare declared target-suffixed Leaf/HEV externalBin paths. Core, Mobile and Test were cancelled after the real release blocker was confirmed; OHOS passed. No tag or Release was created. |
| Complete replacement diff review | PASS | The complete post-`f45fad64` batch preserves `a480e6f5` and `b198d3bc`; only superseded post-build documentation commit `1ad3c0d3` is outside the ancestry. Exact Leaf `682d1dc4`, rust-tun `028b861d`, HEV `97e74f10`, platform `cfg`, formal real-sidecar boundaries and changed-workflow syntax agree. |
| Remote Rust preflight | PASS | The final `.160` locked no-run build resolved `easytier-ace1b86550e841ce`, `easytier_policy-545376a733078812`, `easytier_socks_egress-12f3e3377414aaed`, and `netstack_smoltcp-7d3834b0fcf73e6b`; every configured serial filter passed. The unchanged broad three-node test still exposes only its documented ignored cleanup assertions, so exact-artifact lifecycle checks remain authoritative. |
| Remote HEV wrapper link gate | PASS | `.160` debug `cargo build --locked` linked the wrapper with pinned HEV `97e74f1068bd...`; x86_64 musl was runnable static PIE and `--version` returned the full revision. |
| Remote Test/Clippy blocker reproduction | PASS | With the same Linux GUI dependencies as `prepare-build(gui=true)` and only the two host-target Tauri fixtures, exact `cargo clippy --locked --all-targets --features full --all -- -D warnings` passed in 2m58s. The fixtures are not packaged; formal GUI builds and inspects real sidecars. |
| Remote frontend/GUI preflight | PASS | Node 22 frozen install, focused PolicyEditor/runtime/remote-management Vitest 43/43, frontend-lib, Web, VPN plugin declarations and GUI builds passed in dependency order. |
| Static workflow and package audit | PASS | Rust formatting, diff whitespace, preflight shell syntax and changed-Test `actionlint` passed. Frozen pnpm/Cargo gates, complete workflow inputs, artifact names, real-sidecar builders and all HEV pins were rechecked; Windows compilation remains formal-workflow-only because `.160` has no Windows SDK. |
| Linux profiling replacement workflow | BLOCKED | Requires the immutable replacement candidate SHA. |
| Android policy replacement workflow | BLOCKED | Requires the immutable replacement candidate SHA. |
| Linux exact-artifact validation | BLOCKED | Requires the replacement profiling artifact, sidecar/pin checks, loopback-only target HEV listener, managed HEV cross-peer TCP/UDP, candidate-port fallback and lifecycle cleanup. |
| macOS focused `a480e6f5` package and device | PASS | Dedicated run `29997237387`; exact arm64 DMG SHA-256 `dd7bb7055174b072ab5a4707393f902cff54c3126917a903ef3e73e7bb17da52`, strict app/core/Leaf/HEV signatures, ten rounds of initial/recovered TCP+UDP and 20 complete 4 MiB pressure transfers, no targeted drops/errors, bounded route recovery and clean shutdown. |
| macOS additional Release-artifact device validation | WAIVED_BY_MAINTAINER | The maintainer will validate the published Release artifact. Formal Core/GUI payload manifests, architecture, executable permissions, sidecars and signatures remain mandatory before Release. |
| Android physical device | BLOCKED | The prior successful evidence was exact to `f45fad64`, before the shared HEV default changed. The replacement APK requires preserved-data upgrade, captured-UID TLS, managed HEV and lifecycle validation. |
| Core formal workflow | BLOCKED | Authorized after the final Linux/Android exact-artifact gates pass. |
| GUI formal workflow | BLOCKED | Authorized after the final Linux/Android exact-artifact gates pass. |
| Mobile formal workflow | BLOCKED | Authorized after the final Linux/Android exact-artifact gates pass. |
| OHOS formal workflow | BLOCKED | Authorized after the final Linux/Android exact-artifact gates pass. |
| Test formal workflow | BLOCKED | Authorized after the final Linux/Android exact-artifact gates pass. |
| Tag and GitHub Release | BLOCKED | Requires all formal workflows for one frozen release SHA and inspected formal payloads. |
