# v3.0.5 Validation Matrix

| Gate | State | Exact evidence |
| --- | --- | --- |
| Previous candidate `046ce631` | REJECTED | Profiling payload was policy-capable, but formal Core/GUI payloads omitted required features or sidecars. Its successful workflow and Linux results are retained only as diagnostic evidence. |
| Complete replacement diff review | PASS | All 27 workspace paths reviewed: runtime changes are limited to HEV supervision/config propagation and prepare error classification; remaining build changes align formal Core/GUI/Magisk payloads and gates. Two pre-existing safe TODO documents are intentionally included under the workspace submission rule. Credential/private-host scan was clean. |
| Remote Rust preflight | PASS | Complete final Rust tree passed `.160` locked no-run and the configured serial EasyTier/policy/netstack focused suite without compiler warnings. |
| Remote HEV wrapper link gate | PASS | `.160` debug `cargo build --locked` linked the wrapper with pinned HEV `97e74f1068bd...`; x86_64 musl was runnable static PIE and `--version` returned the full revision. |
| Remote frontend/GUI preflight | PASS | Node 22 PolicyEditor Vitest 19/19 plus frontend-lib, Web, VPN plugin and GUI production builds passed in dependency order with the frozen lockfile. Extra Rust GUI check is builder-blocked only by its known missing `gdk-3.0`; macOS workflow remains authoritative. |
| Linux profiling replacement workflow | BLOCKED | Requires the immutable replacement candidate SHA. |
| Android policy replacement workflow | BLOCKED | Requires the immutable replacement candidate SHA. |
| Linux exact-artifact validation | BLOCKED | Requires the replacement profiling artifact and sidecar payload checks. |
| macOS ARM64 package and device | BLOCKED | Requires the dedicated exact-SHA workflow artifact, strict signature checks, installed managed HEV TCP/UDP and parent-death cleanup. |
| Android physical device | BLOCKED | The prior one-time waiver was exact to `046ce631` and does not apply to the replacement candidate. |
| Core formal workflow | BLOCKED | Requires maintainer confirmation after exact-artifact validation. |
| GUI formal workflow | BLOCKED | Requires maintainer confirmation after exact-artifact validation. |
| Mobile formal workflow | BLOCKED | Requires maintainer confirmation after exact-artifact validation. |
| OHOS formal workflow | BLOCKED | Requires maintainer confirmation after exact-artifact validation. |
| Test formal workflow | BLOCKED | Requires maintainer confirmation after exact-artifact validation. |
| Tag and GitHub Release | BLOCKED | Requires all formal workflows for one frozen release SHA and inspected formal payloads. |
