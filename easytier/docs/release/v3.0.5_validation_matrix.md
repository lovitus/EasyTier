# v3.0.5 Validation Matrix

| Gate | State | Exact evidence |
| --- | --- | --- |
| Previous candidate `046ce631` | REJECTED | Profiling payload was policy-capable, but formal Core/GUI payloads omitted required features or sidecars. Its successful workflow and Linux results are retained only as diagnostic evidence. |
| Previous replacement `316bfb69` | SUPERSEDED | Its Linux, Android and macOS exact-artifact checks passed and remain valid historical evidence, but they do not validate the later desktop Leaf/rust-tun changes. |
| Frozen replacement `f45fad64` | REJECTED | Exact Linux and Android candidate checks passed, but formal GUI failed on the Windows policy path because `Duration` was unavailable under Windows `cfg`. Test/Clippy independently failed because the static Tauri check did not prepare declared target-suffixed Leaf/HEV externalBin paths. Core, Mobile and Test were cancelled after the real release blocker was confirmed; OHOS passed. No tag or Release was created. |
| Runtime-validated replacement `3f3c93e3` | FORMAL_GATE_FAILED | Profiling run `30012961724`, Android run `30012964902`, Linux and Android physical validation passed. Formal GUI `30016960146`, Mobile `30016963224`, OHOS `30016966315` and Test `30016969776` passed. Core `30016957223` built Windows x86_64 successfully but its pre-package Git-Bash smoke returned `127` before target DLLs were staged; fail-fast cancelled unfinished Core targets. No tag or Release was created. |
| Post-`3f3c93e3` workflow-only waiver | APPROVED | Maintainer waived another profiling/Android candidate build and full Linux/Android device cycle because the delta only stages the already packaged Windows runtime files before the formal Core smoke and switches that smoke to native PowerShell diagnostics. Runtime source, lockfile, features, build commands and archive collection are unchanged. All five formal workflows must still pass on one final SHA; new formal artifacts still require static inspection plus short Linux/Android installed smokes. |
| Complete replacement diff review | PASS | The complete post-`f45fad64` batch preserves `a480e6f5` and `b198d3bc`; only superseded post-build documentation commit `1ad3c0d3` is outside the ancestry. Exact Leaf `682d1dc4`, rust-tun `028b861d`, HEV `97e74f10`, platform `cfg`, formal real-sidecar boundaries and changed-workflow syntax agree. |
| Remote Rust preflight | PASS | The final `.160` locked no-run build resolved `easytier-ace1b86550e841ce`, `easytier_policy-545376a733078812`, `easytier_socks_egress-12f3e3377414aaed`, and `netstack_smoltcp-7d3834b0fcf73e6b`; every configured serial filter passed. The unchanged broad three-node test still exposes only its documented ignored cleanup assertions, so exact-artifact lifecycle checks remain authoritative. |
| Remote HEV wrapper link gate | PASS | `.160` debug `cargo build --locked` linked the wrapper with pinned HEV `97e74f1068bd...`; x86_64 musl was runnable static PIE and `--version` returned the full revision. |
| Remote Test/Clippy blocker reproduction | PASS | With the same Linux GUI dependencies as `prepare-build(gui=true)` and only the two host-target Tauri fixtures, exact `cargo clippy --locked --all-targets --features full --all -- -D warnings` passed in 2m58s. The fixtures are not packaged; formal GUI builds and inspects real sidecars. |
| Remote frontend/GUI preflight | PASS | Node 22 frozen install, focused PolicyEditor/runtime/remote-management Vitest 43/43, frontend-lib, Web, VPN plugin declarations and GUI builds passed in dependency order. |
| Static workflow and package audit | PASS | Rust formatting, diff whitespace, preflight shell syntax and changed-Test `actionlint` passed. Frozen pnpm/Cargo gates, complete workflow inputs, artifact names, real-sidecar builders and all HEV pins were rechecked; Windows compilation remains formal-workflow-only because `.160` has no Windows SDK. |
| Linux profiling replacement workflow | PASS | Immutable SHA `3f3c93e32bf2a2d0249ea7e7b098ad91998151eb`; run `30012961724`. Outer and inner SHA-256 manifests, `BUILD_INFO.txt`, target, symbols, Build IDs, four executable payloads and HEV pin `97e74f1068bd924e740032382cdc94ca83741ae6` passed. |
| Android policy replacement workflow | PASS | Same immutable SHA; run `30012964902`. APK, target/runner probes, SHA-256 manifest, target, stable signing identity and matching HEV pin passed. |
| Linux exact-artifact validation | PASS | CentOS 7 exact-artifact runs proved loopback-only mapped listener `[::ffff:127.0.0.1]:11081` after occupying `11080`; cross-peer TCP/UDP, target-side egress source, exit restart, old-HEV removal and host-state cleanup passed. The rejected `f45fad64` artifact exposed `[::ffff:0.0.0.0]:11081` and failed the same listener gate. A separate `socket-mark=77` run counted 13,212 target packets / 34,076,668 bytes with mark `0x4d`. The 16 MiB DIRECT plus native/KCP/QUIC/relay profiling self-test passed all watchdog, byte, resource and cleanup gates. |
| macOS focused `a480e6f5` package and device | PASS | Dedicated run `29997237387`; exact arm64 DMG SHA-256 `dd7bb7055174b072ab5a4707393f902cff54c3126917a903ef3e73e7bb17da52`, strict app/core/Leaf/HEV signatures, ten rounds of initial/recovered TCP+UDP and 20 complete 4 MiB pressure transfers, no targeted drops/errors, bounded route recovery and clean shutdown. |
| macOS additional Release-artifact device validation | WAIVED_BY_MAINTAINER | The maintainer will validate the published Release artifact. Formal Core/GUI payload manifests, architecture, executable permissions, sidecars and signatures remain mandatory before Release. |
| Android physical device | PASS | USB device exact-APK preserved-data upgrade retained `firstInstallTime` and byte-identical shared preferences, WebView Local Storage and IndexedDB before first launch. An isolated Linux policy client selected Android as a portless managed HEV peer: TCP was 18.35 Mbit/s, UDP was 4.84 Mbit/s with 0% loss, `/proc/net/tcp6` exposed only `::ffff:127.0.0.1:11080`, and the controlled target observed Android-side source `192.168.6.36`, not the Linux client. Missing actor failed closed; disable removed VPN/HEV/traffic; same-process enable recovered. Exact captured-UID TLS passed VPN-down and `MATCH,DIRECT`, while `MATCH,REJECT` retained the local TCP handshake but blocked TLS as required. Probe packages and isolated topology were removed; original backend config, ordered local network list, VPN address and TLS behavior were restored. |
| Core formal workflow | BLOCKED | Post-`3f3c93e3` SHA must stage target runtime payload before its native Windows smoke and complete every target successfully. |
| GUI formal workflow | BLOCKED | Run `30016960146` passed for `3f3c93e3`; the final post-smoke-repair SHA still requires its own successful formal run and payload inspection. |
| Mobile formal workflow | BLOCKED | Run `30016963224` passed for `3f3c93e3`; the final post-smoke-repair SHA still requires its own successful formal run and payload inspection. |
| OHOS formal workflow | BLOCKED | Run `30016966315` passed for `3f3c93e3`; the final post-smoke-repair SHA still requires its own successful formal run and payload inspection. |
| Test formal workflow | BLOCKED | Run `30016969776` passed for `3f3c93e3`; the final post-smoke-repair SHA still requires its own successful formal run. |
| Tag and GitHub Release | BLOCKED | Requires all formal workflows for one frozen release SHA and inspected formal payloads. |
