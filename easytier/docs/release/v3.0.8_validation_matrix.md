# v3.0.8 Validation Matrix

This file defines the release gates. Results are recorded only after the exact
candidate SHA and immutable artifacts exist.

| Gate | Status | Required evidence |
| --- | --- | --- |
| Complete replacement candidate diff | PASS | The follow-up changes only the retry-table `AtomicU64` import to the existing locked `atomic-shim`; version/schema/lockfile/platform/workflow audit passed. |
| `.160` Rust preflight | PASS | Replacement snapshot: locked no-run passed for `easytier` and `easytier-web`; endpoint 8/8 and bypass/global-context/CLI 3/3 passed without warnings. |
| `.160` frontend/GUI preflight | PASS | Replacement snapshot: Config/RemoteManagement 29/29, generated full-field round-trip, and dependency-ordered frontend-lib, frontend, VPN plugin and GUI builds passed. |
| Profiling candidate artifact | BLOCKED | Requires a new immutable replacement SHA and exact artifact audit. |
| macOS ARM64 candidate artifact | BLOCKED | Requires a new immutable replacement SHA and exact artifact audit. |
| Android policy candidate artifact | BLOCKED | Requires a new immutable replacement SHA; compile/package evidence only. |
| Android physical device | WAIVED_BY_MAINTAINER | One-release exception explicitly accepted by the maintainer on 2026-07-30. The unavailable device was not reconnected, installed, cleared or mutated. |
| Linux exact-artifact validation | BLOCKED | The first candidate passed the full matrix, but the replacement requires an exact-artifact smoke before that evidence can be reused as unchanged-behavior support. |
| macOS exact-artifact validation | BLOCKED | The first candidate passed the full GUI/A/B matrix, but the replacement requires an exact installed-artifact smoke. |
| Formal Core workflow | BLOCKED | Run only after maintainer confirms exact-artifact validation. |
| Formal GUI workflow | BLOCKED | Run only after maintainer confirms exact-artifact validation. |
| Formal Mobile workflow | BLOCKED | Compile/regression gate; Android physical validation is waived for this release. |
| Formal OHOS workflow | BLOCKED | Compile/regression gate. |
| Formal Test workflow | BLOCKED | Full repository test and lint gate. |
| Formal artifact audit | BLOCKED | Required flags, sidecars, architecture, modes, signatures and installed-artifact smokes match candidate. |
| Release | BLOCKED | Tag and assets must target the single validated formal-artifact SHA. |

## Superseded first-candidate evidence

First candidate SHA: `37fef1b4b040a857b6aa3c6f48b80c83fa4a5a1f`

Formal Core run `30503244034` failed the actual
`mipsel-unknown-linux-musl` build because the retry table imported
`std::sync::atomic::AtomicU64`, which that target does not provide. The
replacement uses the workspace's existing `atomic-shim::AtomicU64`; therefore
the following first-candidate evidence remains diagnostic and behavioral
support but is not release acceptance for the replacement SHA.

- Candidate workflows:
  - Linux profiling beta `30495990595`: PASS;
  - Android policy candidate `30495990624`: PASS (compile/package only);
  - macOS ARM64 GUI candidate `30495990563`: PASS.
- Internal Linux direct path:
  - exact core SHA256
    `f7cfe76e72ddfd41d45636715fe1fb7a3f5ef6b117b027c91089182029b106ef`;
  - both directions passed 4/4 with 0% loss;
  - initial UDP connectivity upgraded to priority QUIC in about four seconds;
  - blocking only the isolated QUIC test ports produced the expected detection
    interval, then the existing UDP path carried 20/20 packets;
  - after removing the rules, QUIC was established again and 5/5 passed.
- Three-physical-host relay path:
  - both endpoints ran with `disable_p2p=true`, so no endpoint-to-endpoint
    Direct tunnel was permitted;
  - initial forward relay traffic passed 5/5 at about 335 ms;
  - stopping the relay produced the controlled 3/3 outage;
  - ten seconds after the relay restart, forward 3/3 and reverse 3/3 passed at
    about 332 ms, and the relay recorded both endpoint reconnections.
- Public dual-stack path:
  - the initial peer used a global IPv6 UDP underlay;
  - overlay IPv4 and IPv6 each passed 5/5 with 0% loss;
  - all test cores, TUNs and temporary firewall rules were removed afterward.
- macOS GUI forced-P2P comparison:
  - the generated runtime configuration was checked in each phase:
    `lazy_p2p=false`, `need_p2p=true`, and opt-out false/true as intended;
  - with throttling enabled, the 90-second window averaged 193.54 active
    connections and created 598 connection IDs (6.644/s), with peak 316;
  - with throttling disabled, the same-length window averaged 278.44 active
    connections and created 691 connection IDs (7.678/s), with peak 354;
  - in the following 180-second steady-state windows, throttling enabled
    created 319 IDs (1.772/s), while opt-out created 871 IDs (4.839/s);
  - both steady-state windows reached the same 74 unique endpoints, so the
    throttle reduced repeated attempts rather than candidate coverage;
  - the enabled throttle reduced steady-state new connections by 63.4%;
  - GUI/Mihomo remained running throughout each bounded measurement.
- macOS restoration:
  - the compatibility switch, `lazy_p2p` and `need_p2p` were returned through
    the GUI to their original values;
  - v3.0.7 GUI and Mihomo were restarted;
  - the restored runtime TOML SHA256 is
    `f4f99f71c6e787ccbd07469e9a78a37ba39588e6aee912dd2ecf3799024e77f2`,
    byte-identical to the pre-test backup.
