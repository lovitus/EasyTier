# v3.0.9 Validation Matrix

Candidate SHA: `e40486aa65dfe41bef314bad2ae23369ac1e658d`.

| Gate | Status | Required evidence |
| --- | --- | --- |
| Latest post-v3.0.8 base | PASS | Candidate begins at `f20c1048`, including commits `12c055b0` and `f20c1048` made after tag `v3.0.8`. |
| Complete candidate diff | PASS | Minimal WG fix, deterministic test, versions, release documentation, lockfile, cfg, workflow and generated-file audit passed. |
| `.160` locked no-run | PASS | Focused and default-feature `easytier --lib` targets plus `easytier-web` built without warnings. |
| `.160` deterministic race | PASS | Peer was removed after capture and before dispatch; listener completed through the captured `Arc`, remained alive, and did not reinsert it, 1/1 on both test binaries. |
| `.160` WG focused suite | PASS | Complete WireGuard module passed 14/14 serially with both focused and default features. |
| `.160` frontend/GUI gate | PASS | Frontend Vitest 120/120; frontend-lib, frontend, VPN plugin, and GUI production builds passed in dependency order. |
| Profiling candidate artifact | PASS | Run `30519170705`; exact SHA, checksums, build info, build ID, symbols, x86_64-musl static-PIE target, executable mode and `3.0.9-e40486aa` version verified. |
| Linux exact-artifact validation | PASS | The exact profiling artifact completed 10 WG teardown/reconnect cycles, 80/80 continuous payload probes, 11 accepts interleaved with 10 removals, and a functional 182 MiB transfer with no panic or listener exit. FD/thread counts returned to 36/11. External Portal smoke was unavailable because the hosts lack the `wg` tool; Portal uses the same tested `WgTunnelListener`, so this is recorded as a tooling limitation rather than independent runtime evidence. |
| Android candidate workflow | PASS | Run `30519170678`; exact-SHA compile/package regression passed. Physical-device validation was unavailable under the current repository instruction. |
| Atomic portability source audit | PASS | All hits classified against locked `atomic-shim 0.2.0`; MIPS-reachable operations exist in its fallback, `fetch_max` is only on `AtomicU32`, and standard `AtomicU64` hits are test-only or excluded from MIPS. Formal Core remains the mandatory target proof. |
| Formal Core | PASS | Run `30520811578`, attempt 2, completed 21/21 on the exact SHA. MIPS and MIPSel artifacts are respectively 32-bit MSB and LSB MIPS32r2 static ELF. Attempt 1 hit a macOS runner pnpm `ETIMEDOUT`; the same run was rerun unchanged and passed. |
| Formal GUI | PASS | Run `30522227472`; all seven target jobs passed on the exact SHA. |
| Formal Mobile | PASS | Run `30522230293`; all four Android ABI packages and the result gate passed on the exact SHA. |
| Formal OHOS | PASS | Run `30522232649`; formatting and HAR build passed on the exact SHA. |
| Formal Test | PASS | Run `30522235221`; lint, feature checks and all test partitions passed, 10/10 jobs. |
| Formal artifact audit | PASS | Linux x86_64, MIPS/MIPSel, macOS ARM64 and Windows x86_64 Core artifacts were checked for target, mode, static linkage where applicable, sidecars and manifests. The macOS DMG passed deep/app/sidecar signature checks; the Windows x64 installer contained the expected x86_64 policy/runtime payloads. The formal Linux Core ran on CentOS 7 and reported `3.0.9-e40486aa`. |
| Release v3.0.9 | PASS | Run `30526367741`; tag and release target the candidate SHA. The latest, non-draft, non-prerelease release contains 46 non-empty, uniquely named assets. Re-downloaded Linux Core and Windows x64 GUI assets match their audited formal artifacts. |
