# v3.0.16-3 Validation Matrix

- Candidate SHA: `1d3a5de039509da478ab226b845ae64612fd5ca2`
- Candidate tree: `0a312f4ff9366e520ef64f846d03f8717da2918d`
- Version: `3.0.16-3`
- Leaf SHA: `682d1dc43585a703c993e8875fe4e937b1038733`
- HEV SHA: `97e74f1068bd924e740032382cdc94ca83741ae6`
- Release: [v3.0.16-3](https://github.com/lovitus/EasyTier/releases/tag/v3.0.16-3)

| Gate | Status | Evidence |
| --- | --- | --- |
| Complete candidate diff and release inputs | PASS | Immutable SHA and tree above; candidate worktree clean before dispatch. |
| `.160` locked no-run and focused tests | PASS | `release-operator.sh prepare`: locked Rust no-run, maintained focused suite, quinn-udp 8/8, frontend-lib Vitest 122/122, frontend-lib/frontend/VPN plugin/GUI production builds. |
| Linux profiling candidate | PASS | Run `31506627845`; exact x86_64-musl artifact, version and build identity matched the candidate. |
| Android policy candidate | PASS | Run `31506633860`; exact arm64 candidate APK installed and identified as `3.0.16-3`. |
| Linux exact-artifact validation | PASS | Normal Stop persisted the live peer, cached-only restart recovered, empty/equal/subset snapshots did not replace useful state, config remained unchanged, mesh and lifecycle cleanup passed on the Linux matrix. |
| Android physical-device validation | PASS | Exact APK preserved application data; normal Stop persisted the peer; restart with the configured initial peer removed recovered through cache; mesh ICMP was 3/3 with 0% loss; VPN and process cleanup passed. |
| Formal Core including MIPS/MIPSel | PASS | Run `31509219793`; MIPS job `93838989285` and MIPSel job `93838989286` passed; artifacts were respectively 32-bit MSB and LSB MIPS32r2 static executables. |
| Formal GUI | PASS | Run `31509226795`; Linux package and Windows/macOS payload audits passed, including required policy sidecars and compliance metadata. |
| Formal Mobile | PASS | Run `31509232365`. |
| Formal OHOS | PASS | Run `31509237337`. |
| Formal Test | PASS | Run `31509243093`; original eventless three-node recovery semantics remained intact. |
| Release | PASS | Run `31516023459`; tag and Release target the candidate SHA, Release is published as a prerelease with 46 assets, and post-release Linux/Windows smokes passed. |

## Exact-artifact Linux and Android evidence

- Linux profiling artifact and formal Linux Core artifact both reported
  `3.0.16-3-1d3a5de0`.
- The published Linux x86_64 ZIP SHA-256 is
  `7f95ef391b501b7067908710a1ae3b11e0b99217b24e7e145b000c1b3b878a81`.
  It matched the GitHub asset digest, passed `unzip -t`, and its Core and CLI executed on
  CentOS 7 with the exact candidate version.
- Android candidate APK SHA-256 was
  `417f90101822acd5f399ecd8cb91a52d3abc51c752bca20709b523e8c1fb9946`.
- Android cached-only recovery completed in six seconds after the configured peer list was
  removed through the real UI and saved. Canonical saved configuration was byte-equivalent before
  and after runtime use.
- Quick Android start/stop preserved the cache hash and nanosecond mtime, created no unnecessary
  backup, and left no active candidate VPN.

## Formal artifact and release audit

- The formal Linux x86_64 package contained Core, CLI, Mihomo, GOST, Leaf worker, HEV and all
  expected compliance metadata. Core/CLI and sidecar manifest hashes matched.
- Both formal MIPS artifacts were audited for architecture and endianness, not inferred from an
  x86 build or source grep.
- Remote tag `v3.0.16-3` resolves to the exact candidate SHA.
- GitHub Release metadata: published `2026-08-11T17:12:04Z`, not a draft, prerelease enabled.
- All 46 assets expose GitHub SHA-256 digests. Independently downloaded checks:
  - Linux x86_64 ZIP:
    `7f95ef391b501b7067908710a1ae3b11e0b99217b24e7e145b000c1b3b878a81`
  - Windows x64 ZIP:
    `b0373fb7b3a4e98bae015412e58809e3181d1b07935cbe3c207853412abf02bf`
  - Windows x64 installer:
    `b1df604b998829974790532fd78a4cd9256567ff5f80da1cc19f5b53b8ad017a`

## Post-release Windows closeout

- Host `192.168.234.207` was upgraded from GUI `3.0.14` to the exact published
  `3.0.16-3` installer. Installer exit was zero and the two authoritative source configuration
  files retained identical SHA-256 values.
- The desktop session was disconnected, so a WebView GUI automation run was deliberately rejected
  as invalid evidence. It was cleaned without changing source configuration or producing a new
  panic log.
- The exact published Windows x64 Core/CLI ZIP reported `3.0.16-3-1d3a5de0`.
- A temporary Windows Service used explicit `27910-27914` listeners and connected over TCP to an
  exact published Linux Core. The first normal Service Stop wrote one primary cache containing the
  live peer URL.
- The service was then reinstalled with no `--peers` argument. It recovered solely through the
  cached URL, reached the Linux peer at 1.69 ms with 0% loss, and completed a second normal Stop.
- The second identical snapshot preserved both cache SHA-256 and mtime, created no backup, and did
  not rewrite the primary. The temporary service, Windows Core process and Linux validation server
  all had zero residuals.

Manual pre-release runtime acceptance was intentionally limited to Linux and Android. Windows was
used only for the requested post-release closeout. Initial-node exponential cooldown remains
excluded; v3.0.16-2 connector retry and eventless recovery semantics are unchanged.
