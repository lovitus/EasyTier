# v3.0.16-4 Candidate Manifest

- Status: RELEASED
- Candidate SHA: `391c191c3d8b477c3b7e8ef19e87ae3cba5c9504`
- Candidate tree: `0c4e164e4f01d02c33199dc30e1802752d07f761` (1083 tracked files)
- Scope: distinguish persisted bootstrap helpers from configured connectors and pause only helper
  reconnect scheduling while the primary peer map already has a live connection.
- Leaf SHA: `682d1dc43585a703c993e8875fe4e937b1038733`
- HEV SHA: `97e74f1068bd924e740032382cdc94ca83741ae6`
- Android candidate: required
- Preflight: `scripts/release-operator.sh prepare`
- Validation: `scripts/release-operator.sh validate`

The exact run IDs, artifact identities and post-publication smoke evidence are recorded in the
validation matrix.

## Frozen implementation scope

- Register configured and RPC-added connectors with their existing persistent retry semantics.
- Register persisted-only bootstrap URLs as per-instance runtime helpers without modifying user
  configuration or the on-disk bootstrap format.
- Stop scheduling new helper reconnect tasks while the primary peer map contains any registered,
  non-closed connection; resume on the existing manager tick when that map becomes empty.
- Allow an already in-flight helper attempt to finish once rather than introducing cancellation or
  another lifecycle state machine.
- Linearize Add, Remove, Clear, reconnect claim and reconnect completion so an in-flight task cannot
  resurrect a removed connector or overwrite a newer source classification.
- Leave foreign-network peer maps, connector transports, authentication, routing, packet forwarding,
  P2P discovery, QUIC, KCP and FakeTCP unchanged.
- Add no timer, event subscriber, cooldown sequence, persistent health state, configuration field or
  public API.

## Pre-build evidence

- Local formatting and `scripts/pre-commit-check.sh` passed.
- `.160` locked no-run and the complete maintained Leaf/HEV preflight passed.
- The original TCP and WireGuard three-node disconnect/recovery test passed 2/2 and its source blob
  remains byte-identical to the parent commit.
- The helper lifecycle/source tests passed five consecutive runs each; the seven manual connector
  tests passed three concurrent rounds; the live-peer pause/eventless-resume test passed repeated
  serial runs.
- Multiple independent read-only reviews found no P1/P2 issue after Add/Remove/Clear and in-flight
  reconnect races were covered.

## Published result

- Release: `v3.0.16-4`, published as a prerelease from the exact candidate SHA.
- Candidate runs: Linux `31561399937`; Android `31561403481`.
- Formal runs: Core `31562862840`; GUI `31562865807`; Mobile `31562868571`; OHOS
  `31562871068`; Test `31562873972`.
- Release run: `31567138191`.
- The earlier `598707d58ba2f2f77677c7f631d344f1f959902e` candidate is rejected release
  evidence: formal Test correctly found a Clippy error before publication. The one-line,
  behavior-neutral fix was included in the final candidate, then all candidate, device and formal
  gates were repeated for the new SHA.
