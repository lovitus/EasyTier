# v3.0.6 Candidate Manifest

## Immutable source boundary

- Release base: `v3.0.5` (`6cc96616c8e466c7a8d852761fed43603206fce2`).
- Runtime-validated implementation parent:
  `d72b30f09a3930e23b02958f481370282798cd79`.
- Version: `3.0.6`.
- Leaf SHA: `682d1dc43585a703c993e8875fe4e937b1038733`.
- HEV SHA: `97e74f1068bd924e740032382cdc94ca83741ae6`.
- Candidate branch: `codex/v3.0.6-release`.
- Final SHA is the single head SHA recorded by the profiling, Android
  candidate, Core, GUI, Mobile, OHOS, Test, and Release runs.

The delta after the runtime-validated parent is restricted to version metadata,
the corresponding lockfile entries, the Release default, release documents,
version-independent release-audit selection, and selecting the workflow's
existing `cargo zigbuild` path for the FreeBSD GOST guardian. The vendored
Quinn UDP imports are formatted exactly as required by the OHOS sub-workspace's
Rust 1.95 `cargo fmt --all`. The final formal-gate repair after
`79f67f2c4b538991a0ed87e81ff424f33c26c118` additionally:

- records and verifies the exact ad-hoc-codesigned Darwin GOST bytes while
  retaining the workflow-pinned upstream archive and original binary hashes;
- fixes the GUI compliance check to inspect the workspace `target` directory;
- fixes the `mesh-socks-egress` single-feature import boundary;
- updates the no-runtime test to assert the already-implemented rollback of
  instances whose lifecycle watcher cannot be registered; and
- applies only Rust 1.95 Clippy-equivalent, behavior-preserving cleanup,
  including an explicit non-truncating macOS ownership-lock open.

No mesh, policy, GOST, or Mihomo data-plane behavior is changed by this final
gate repair.

## Included implementation

- Decoupled Mihomo policy backend and generated runtime configuration.
- Core-owned GOST TCP/UDP neutral mesh entry and supervised lifecycle.
- Exact-virtual-IP desktop TCP ingress with bounded relay memory.
- macOS DNS/interface recovery and QUIC truncation/starvation fixes.
- GUI policy backend validation, compact editing, and runtime status.
- Three-node readiness stabilization and Core traffic-metric hot-path cleanup.
- Formal Mihomo/GOST/Leaf/HEV packaging and release payload verification.

## Required pre-build gates

- `.160` locked Rust no-run plus the complete focused policy/network suite.
- `.160` Node 22 focused frontend tests and dependency-order production builds.
- Lockfile, dependency pins, platform `cfg`, workflow pins, credentials, and
  complete candidate-diff review.
- `actionlint` for the FreeBSD guardian workflow correction. The FreeBSD
  target/sysroot exists only in the formal GitHub matrix; its exact guardian
  build is the required target-specific evidence.
- OHOS sub-workspace `cargo fmt --all -- --check`.
- Final repair preflight on `.160`: standard locked no-run and focused suite
  passed; `cargo clippy --locked --all-targets --features full --all --
  -D warnings` passed; all 28 `easytier` feature combinations passed (the
  first 21 in the bounded `cargo hack` run and the remaining seven in an
  explicit continuation after the 30-minute command timeout); and
  `watcher_registration_failure_rolls_back_the_new_instance` passed 1/1.
- Darwin GOST transform test passed for unsigned verification, rejection before
  recording a signed hash, recorded signed verification, stable repeat
  ad-hoc signing, and tamper rejection. `bash -n` and `actionlint` passed.
- One profiling candidate and one Android policy candidate for the final SHA.

## Required release gates

- Formal Core, GUI, Mobile, OHOS, and Test workflows all succeed for one SHA.
- Core and GUI artifacts contain the declared Mihomo/GOST/Leaf/HEV payloads,
  metadata, architecture, permissions, and macOS signatures.
- Release workflow resolves those exact five successful runs and publishes
  `v3.0.6` without replacing any existing tag.
- Published tag, target, assets, checksums, internal versions, and one installed
  artifact smoke are verified after release.

## Platform boundary

The exact Linux x86_64-musl implementation parent passed dual-stack,
TCP/UDP, restart, resource, and cleanup validation. Android retains Leaf/HEV
and is not evidence for Mihomo. macOS, Windows, FreeBSD, and unsupported Mihomo
target mappings remain limited to their explicitly recorded build/runtime
evidence.
