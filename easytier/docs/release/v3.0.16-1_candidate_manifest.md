# v3.0.16-1 Candidate Manifest

Status: READY_FOR_CANDIDATE_COMMIT

## Purpose

Remove the configured initial node as a permanent single point of failure after
an EasyTier network has successfully joined the mesh at least once. A normal
Stop or Exit records the currently live outbound peer URLs, and the next start
uses that auxiliary snapshot as additional runtime-only bootstrap input.

## Immutable scope

- Base implementation: `7d5a4a08105a4e1fe803d8bcdf2db92a30f18f42`.
- Version: `3.0.16-1` on every Cargo, Tauri, GUI and Android delivery path.
- Leaf SHA: `682d1dc43585a703c993e8875fe4e937b1038733`.
- HEV SHA: `97e74f1068bd924e740032382cdc94ca83741ae6`.
- Save only live outbound EasyTier connection URLs during normal Stop or Exit.
- Prefer the original remote URL, fall back to the resolved URL, and never
  persist process-local `ring://` endpoints.
- Keep one small text file per stable network identity under the existing
  platform-managed configuration directory.
- Append valid cached URLs in memory after configured initial peers with exact
  deduplication; never rewrite TOML, GUI state, imported files or serialized
  initial peers.
- Replace the cache only with a non-empty live snapshot. An empty snapshot does
  not erase the last useful auxiliary data.
- Share the manager implementation across Core, GUI/Tauri, Android JNI, FFI and
  OHOS entry paths. No packet-path, routing, transport or protocol behavior is
  changed.
- Excluded: timers, peer scoring, TTL, database state, crash-time persistence,
  automatic configuration migration and packet hot-path work.

## Build-affecting implementation

- `easytier/src/peers/peer_map.rs`
- `easytier/src/instance/instance.rs`
- `easytier/src/launcher.rs`
- `easytier/src/instance_manager.rs`
- `easytier-contrib/easytier-ffi/src/instance_api.rs`
- `easytier-contrib/easytier-ffi/src/lib.rs`
- `easytier-contrib/easytier-android-jni/src/network_api.rs`
- `easytier-contrib/easytier-android-jni/src/lib.rs`
- `easytier-contrib/easytier-android-jni/kotlin/com/easytier/jni/EasyTierJNI.kt`
- `easytier-contrib/easytier-android-jni/kotlin/com/easytier/jni/EasyTierManager.kt`
- `easytier-contrib/easytier-ohrs/src/lib.rs`

## Pre-build gates

1. Local Rust 2024 formatting and `scripts/pre-commit-check.sh`.
2. `scripts/release-operator.sh builder-preflight` on `192.168.2.160`.
3. The six focused bootstrap snapshot contracts in the exact EasyTier library
   test binary.
4. Locked no-run coverage for EasyTier FFI and Android JNI; OHOS remains a
   dedicated workflow target because it owns an independent workspace lock.
5. Complete diff, lockfile, dependency pin, platform `cfg`, generated-file,
   credential and 64-bit atomic portability audit.

## Pre-build result

PASS on 2026-08-10.

- `scripts/pre-commit-check.sh` passed after restoring the installed
  `~/.cargo/bin` directory to the Codex command environment `PATH`; no check was
  bypassed or relaxed.
- `scripts/release-operator.sh builder-preflight` passed on
  `192.168.2.160`. Its first attempt completed both locked no-run builds before
  a new SSH connection timed out; direct reachability immediately recovered,
  and the unchanged snapshot passed the complete incremental rerun.
- Exact EasyTier library test binary
  `/workspace/target/debug/deps/easytier-d4185b143dc2e052` passed the default
  Leaf/HEV focused suite, including all six bootstrap snapshot contracts.
- Three-node ACL, netstack backpressure/lifecycle, policy DNS/configuration and
  QUIC UDP tests passed.
- Frontend-lib Vitest passed 122 tests in 15 files. Frontend-lib, frontend, VPN
  plugin and GUI production builds passed in the required dependency order.
- The complete candidate file list, lockfile, Leaf/HEV pins, platform boundary,
  generated files and 64-bit atomic portability classification were audited.
  A scan of the complete patch found no private validation-host metadata or
  validation credentials.

## Candidate and validation plan

1. Dispatch Linux Profiling Beta and Android Policy Candidate together for the
   immutable release SHA through `scripts/release-operator.sh`.
2. Verify outer and inner checksums, build identity, version, target and pinned
   dependencies for both artifacts.
3. Re-run bounded Linux lifecycle/bootstrap fallback and Android physical-device
   start, cached-peer recovery, normal stop, configuration-retention and cleanup
   checks using the exact versioned artifacts.
4. Run Core, GUI, Mobile, OHOS and Test together for the same SHA.
5. Require both exact-SHA MIPS and MIPSel Core jobs, formal artifact payload
   inspection and installed/archive smoke before Release.
6. Publish `v3.0.16-1` as a prerelease and verify tag, target, assets, checksums,
   internal versions and one formal installation path.

## Prior implementation evidence boundary

Implementation SHA `7d5a4a08` passed `.160`, Linux Profiling Beta run
`31389702233`, Android Policy Candidate run `31389702278`, a Linux three-node
configured-bootstrap outage test, and a physical Android cached-peer recovery
test. This evidence establishes implementation readiness but does not replace
the exact `3.0.16-1` release-SHA gates required above.

## 64-bit atomic portability classification

- MIPS/MIPSel-reachable production 64-bit atomics use `atomic_shim` in
  `secure_datagram`, `peer_task`, `proxy_failover`, `p2p_endpoint_retry`,
  `peer_ospf_route`, `stealth`, `network` and `token_bucket`.
- Standard-library `AtomicU64` occurrences in `peer_conn` and
  `tunnel/common.rs` are test-only.
- Standard-library `AtomicU64` in `virtual_nic.rs` is gated by
  `leaf-policy-proxy` and excluded from both formal MIPS targets.
- The `fetch_max` occurrence operates on `AtomicVersion(AtomicU32)`; no
  `atomic_shim::AtomicU64::fetch_max` call exists.

## Evidence boundary

The release SHA, workflow run IDs, artifact hashes and final measurements do not
exist in this pre-build snapshot. Record them afterward against the immutable
release SHA without redefining or rebuilding it.
