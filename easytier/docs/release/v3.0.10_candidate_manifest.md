# v3.0.10 Candidate Manifest

## Identity

- Base commit: `4a6e0aea97940fd806cd933988dfff29f728e6d0f`
- Base release: `v3.0.9` (`e40486aa65dfe41bef314bad2ae23369ac1e658d`)
- Candidate version: `3.0.10`
- Candidate branch: `codex/v3.0.10-release`
- Formal branch: `releases/3.0.10`
- Candidate SHA: the immutable commit containing this manifest; record the full
  SHA in post-build evidence and use that SHA for every formal workflow and the
  release `artifact_sha` input.

## Build-affecting scope

- Runtime-only Mihomo start, stop, and restart RPCs and GUI actions.
- Existing readiness-state presentation for Mihomo version, mixed/HTTP/SOCKS
  ports, and active TUN device.
- A `Preparing` presentation state while existing Mihomo validation/start work
  is in progress.
- Dynamic Mihomo ownership-token handoff so stale network-stop tasks cannot
  terminate a replacement process.
- Configuration save through the existing network-controller restart path.
- Protobuf, Rust remote client, Tauri command, frontend API, locales, and
  RemoteManagement integration required by those behaviors.
- Version metadata and release documentation for v3.0.10.

## Explicit exclusions

- No mesh data-plane, routing, DNS, GOST protocol, Leaf policy, proxy-rule, or
  Mihomo YAML semantic change.
- No experimental `tools/` code, local artifacts, caches, credentials, or
  private maintainer host metadata.
- No new Mihomo controller polling or resource-sampling loop.

## `.160` pre-push evidence

All commands used the complete synchronized candidate, `--locked` Cargo state,
Node 22, pnpm 9.12.1, bounded timeouts, and the required dependency order.

- Rust lib test no-run: `PASS`, 3m33s.
- `mihomo::tests`: `PASS`, 23/23.
- `instance_manager::tests`: `PASS`, 8/8.
- `remote-management-config.spec.ts`: `PASS`, 21/21.
- frontend-lib production build: `PASS`.
- frontend production build: `PASS`.
- VPN service plugin build: `PASS`.
- GUI production build: `PASS`, final Vite stage 9m09s.
- Version/lock consistency, `git diff --check`, workflow pins, and private-value
  scan: `PASS`.

## Formal workflow plan

Use one immutable SHA for all five mandatory workflows:

- EasyTier Core
- EasyTier GUI
- EasyTier Mobile
- EasyTier OHOS
- EasyTier Test

Push `releases/3.0.10` once at the candidate SHA to start the four release-branch
workflows, and dispatch Test once against the same ref. Do not rebuild for
post-build documentation evidence.

## Exact-artifact validation plan

- Verify workflow head SHA, artifact manifests, checksums, version, target,
  sidecar presence, executable permissions, and platform signatures.
- Exercise runtime stop, start, and restart without changing the persisted
  Mihomo backend or YAML.
- Confirm normal network restart starts Mihomo after a runtime-only stop.
- Confirm status fields come from readiness state and converge from Preparing to
  Running, Stopped, or an actionable error.
- Confirm network stop and application exit leave no owned Mihomo or GOST child.
- Record workflow IDs and exact-artifact outcomes separately against the final
  SHA; evidence-only updates must not move the release branch or tag.
