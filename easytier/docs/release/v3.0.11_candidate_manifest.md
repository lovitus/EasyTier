# v3.0.11 Candidate Manifest

## Scope

- Read-only Mihomo runtime Info popover after Zashboard.
- Controller-reported `bind-address` carried through readiness and protobuf.
- Exact listener endpoint formatting for wildcard, IPv4, IPv6, and custom bind
  values.
- Rust parser and frontend formatter regression tests.
- Version and release documentation for v3.0.11.

## Explicit exclusions

- No Mihomo controller write or hot-update API.
- No new status polling, resource sampler, proxy/rule interpretation, routing,
  DNS, mesh data-plane, GOST, or Leaf behavior.

## Pre-push gates

- `.160` Rust `--locked` no-run: PASS, 3m34s.
- `.160` Mihomo focused tests: PASS, 28/28.
- `.160` instance-manager focused tests: PASS, 8/8.
- `.160` RemoteManagement tests: PASS, 21/21.
- `.160` listener-display tests: PASS, 2/2.
- `.160` frontend-lib, Web frontend, VPN plugin, and GUI production builds:
  PASS; final GUI Vite stage 9m23s.
- Candidate diff, lockfile, generated protobuf, version, and private-value audit:
  PASS before candidate commit.

## Candidate identity

- Branch: `codex/v3.0.11-release`.
- Candidate SHA/tree: record after the immutable candidate commit.
- Required workflows: Core, GUI, Mobile, OHOS, Test against one SHA.
- Exact macOS artifact: verify Info values against authenticated `/configs`,
  then verify normal exit cleanup before Release.
